/* kart.js — kartstøtte er fjernet.
   Leaflet brukes ikke lenger. Eksport-stubber beholder kompatibilitet med eldre imports
   dersom noen glemmer å rydde, men gjør ingenting. */

export function initKart()  {}
export function visKart()   {}
export function skjulKart() {}
